# Smoke task: document the `greet` function

The workspace contains `src/greet.ts` which exports a `greet` function. Add a
single-line JSDoc summary comment directly above the `export function greet`
line. The comment must contain the exact phrase:

    Greets the named user.

Run `run_tests` to verify. Exit code 0 means the task is complete; reply with
`DONE` once tests pass.
